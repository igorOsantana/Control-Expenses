import styled from 'styled-components';

export const ContainerExpenseList = styled.ul`
    list-style: none;
    padding: 0;
`;

export const ExpenseLiseFallBack = styled.h2`
    color: white;
    text-align: center;
`;